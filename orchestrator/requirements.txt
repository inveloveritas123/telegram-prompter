# orchestrator — Python-Abhängigkeiten
# Achtung: httpx nur bei ORCHESTRATOR_ENGINE=claude benötigt (echter MCP-Aufruf)

PyYAML>=6.0
httpx>=0.27.0      # HTTP-MCP-Client (optional, nur bei echter Engine)
pytest>=8.0        # Tests
pytest-asyncio>=0.23
